# ── Dev Dockerfile (for docker-compose dev mode) ──
# Runs as root so mounted volumes are writable.

FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci

EXPOSE 3000

CMD ["npx", "next", "dev"]
